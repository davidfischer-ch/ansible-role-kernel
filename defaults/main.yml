kernel_dynamic_defaults:

  centos:
    kernel_packages:
      - kernel-devel

  debian-8.9:
    kernel_packages:
      - linux-base

  redhat:
    kernel_packages:
      - kernel-headers

  ubuntu-17.*:
    kernel_packages:
      - linux-headers-lowlatency

  ubuntu-20.*:
    kernel_packages:
      - linux-generic

  ubuntu-22.*:
    kernel_packages:
      - linux-base

  ubuntu-24.*:
    kernel_packages:
      - linux-base

kernel_get_config: true
